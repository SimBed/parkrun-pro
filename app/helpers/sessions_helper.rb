module SessionsHelper
  # def clear_session(*args)
  #   args.each do |session_key|
  #     session[session_key] = nil
  #   end
  # end

  # def set_session(prefix, *args)
  #   prefix = "#{prefix}_filter"
  #   args.each do |session_key|
  #     session["#{prefix}_#{session_key}"] = params[session_key] || session["#{prefix}_#{session_key}"]
  #   end
  # end
end
